{
  "dependencies": {
    "@vant/weapp": "^1.10.3"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-postcss": "^9.0.1",
    "postcss-px2units": "^0.2.0"
  },
  "scripts": {
    "build": "gulp css",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
